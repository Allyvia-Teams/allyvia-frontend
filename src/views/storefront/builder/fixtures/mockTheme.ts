/**
 * TEMPORARY mock theme palette for ColorField until T1 theme API is ready.
 */
import type { StorefrontThemePalette } from '../types.local';

export const mockThemePalette: StorefrontThemePalette = {
  ink: '#111827',
  paper: '#FFFFFF',
  surface: '#F8FAFC',
  accent: '#1A73E8',
  line: '#E2E8F0',
  muted: '#64748B'
};
