# allyvia-frontend
Frontend for the Allyvia web app 

# Local Installation
1. Clone repo
2. Create `.env` file and copy + paste content from `.env.example` into `.env`
3. Run ```npm install```
6. Run ```npm start```
7. The frontend should be running at: `localhost:3000/`
